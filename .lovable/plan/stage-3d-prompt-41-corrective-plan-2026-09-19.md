# Stage 3D Prompt #41 corrective plan

## Scope
- Keep Stage 3E unstarted, preserve the Stage 3C 93-case contract, and leave `src/lib/utils.ts` unchanged.
- Use only synthetic disposable runtime infrastructure; never inspect or target protected production society data.

## Changes
1. Remove any verified duplicate or conflicting fixture/test declarations without renaming established public helpers.
2. Keep Stage 3C and Stage 3D behind separate fail-closed gates while sharing only the neutral disposable-environment guard and synthetic fixture factory.
3. Remove fixture teardown attempts that mutate immutable audit history; preserve audit rows until the disposable database is destroyed.
4. Enforce exactly 11 Stage 3D behavioral cases and strengthen report validation for exact totals, failures, pending/skipped cases, and suite setup/teardown failures.
5. Prove the real migration authority from repository tooling and metadata; reconcile the status of migrations 0011/0012 without creating a duplicate migration solely for tests.
6. Validate the final audit contract: unconditional UPDATE/DELETE rejection for every application role, browser append denial, and server-side append only where established.
7. Update only evidence documents whose claims materially change.

## Verification
- Run fresh TypeScript checking immediately after cleanup.
- Run focused Stage 3 runtime, accounting, audit, report, and fixture tests.
- Run all Stage 3 source validators and structural duplicate/count checks.
- Run the full non-live test suite, the guarded live runner, bundle secret scan when an actual build artifact exists, and diff checks.
- Use the automated preview build signal; do not claim disposable runtime, fresh-reset, browser, or security verification without direct evidence.

## Status policy
- Keep Stage 3D `implemented_unverified` unless the disposable 11/0/0/0 runtime and fresh-reset evidence execute successfully.
